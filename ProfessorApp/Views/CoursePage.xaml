<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ProfessorApp.Pages.CoursePage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    BackgroundColor="Black">

    <Grid>

        <!--  Main Content  -->
        <VerticalStackLayout Padding="20" Spacing="10">

            <Label
                Margin="0,20,0,10"
                FontAttributes="Bold"
                FontSize="24"
                HorizontalOptions="Center"
                Text="Courses"
                TextColor="White"
                VerticalOptions="Start" />

            <!-- Courses List -->
            <CollectionView x:Name="CourseCollectionView" SelectionMode="Single" SelectionChanged="OnCourseSelected">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame BackgroundColor="Orange" CornerRadius="8" Padding="10" Margin="0,5">
                            <StackLayout>
                                <Label Text="{Binding CourseName}" TextColor="White" FontAttributes="Bold" />
                                <Label Text="{Binding CourseNumber}" TextColor="White" />
                            </StackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </Grid>
</ContentPage>
